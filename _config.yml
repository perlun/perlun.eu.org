# Dependencies
markdown:         redcarpet
redcarpet:
  extensions:
    - autolink

baseurl:          ""
permalink:        pretty

# Setup
title:            per.halleluja.nu
name:             per.halleluja.nu
paginate:         10

category_archive:
  slugify:        true

exclude: [vendor]

languages: ["xx", "sv", "en"]
exclude_from_localizations: ["images", "public"]

gems:
  - jekyll-multiple-languages-plugin
  - jekyll-paginate
  - jemoji
